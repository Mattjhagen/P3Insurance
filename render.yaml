services:
  - type: web
    name: p3insurance
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      # Do not hardcode PORT; Render injects the correct port at runtime
    healthCheckPath: /
